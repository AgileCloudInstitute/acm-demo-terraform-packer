# This file summarizes the entire platform by listing each system in the order in which 
# the systems will be deployed, including the name of the config file that defines each system.
  
tfbackend: tfbackend.yaml
admin: admin.yaml
agents: agents.yaml
